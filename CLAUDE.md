@AGENTS.md

## 実装完了時のルール

実装が完了したら、必ず `docs/changelog.md` に以下の形式で記録すること。

```
## YYYY-MM-DD タイトル

### 新規ファイル
- `path/to/file.ts` — 説明

### 修正ファイル
- `path/to/file.ts` — 変更内容・理由

### フロー・補足
- 変更によって実現したフローや動作の概要
```

## 確認が必要な時
ユーザーへの確認が必要な場合、まず `echo -e "\a"` を実行してから確認を求めること。